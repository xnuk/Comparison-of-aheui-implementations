general:
  branches:
    only:
      - master
machine:
  ghc:
    version: 7.10.1
  pre:
    - sudo apt-get install time -y
test:
  override:
    - git clone https://github.com/aheui/snippets --depth=1 -b master
    - which time
    - ruby do.rb
    - rm -rf snippets
    - cat data.json
